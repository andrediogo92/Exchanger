syntax = "proto3";

enum type 
{
    DIRECTORY = 0;
    EXCHANGE = 1;
}

message address {
    string host = 1;
    int32 port = 2;
    string name = 3;
    type type = 4;
}

message address_request {
    type type = 0;
    string name = 1;
}
